// import express from "express";
// import {
//   sendRequest,
//   getRequests,
//   approveRequest,
// } from "../controllers/collaborationRequestController.js";
// import { authenticateUser } from "../middleware/authMiddleware.js";

// const router = express.Router();

// // Send a collaboration request
// router.post("/", authenticateUser, sendRequest);

// // Get all collaboration requests for a user
// router.get("/", authenticateUser, getRequests);

// // Approve a collaboration request
// router.put("/:requestId/approve", authenticateUser, approveRequest);

// export default router;
